# Server Configuration
PORT=5000

# MongoDB Connection
# For local: mongodb://localhost:27017/live_polling
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/database_name
MONGODB_URI=mongodb://localhost:27017/live_polling

# Client URLs
# For development: http://localhost:5173
# For production: https://your-frontend-domain.com
# For multiple origins: https://your-domain.com,http://localhost:5173
# For all origins (not recommended in production): *
CLIENT_URL=http://localhost:5173

# Deployment Notes:
# 1. On Render/Railway/Heroku: Set these as environment variables
# 2. Update CLIENT_URL to include your deployed frontend URL
# 3. Ensure MongoDB Atlas allows connections from your deployment IP (0.0.0.0/0 for cloud platforms)
# 4. Use comma-separated values for CLIENT_URL if you need multiple origins
